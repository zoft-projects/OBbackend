export enum PollsEnum {
  Choice = 'Choice',
  Feedback = 'Feedback',
}

export enum OBPollStatusEnum {
  Enabled = 'Enabled',
  Disabled = 'Disabled',
}

export enum PollInteractionStatusEnum {
  Interacted = 'Interacted',
  NonInteracted = 'NonInteracted',
}
