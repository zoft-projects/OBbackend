export enum MessageDirectionEnum {
  Incoming = 'incoming',
  Outgoing = 'outgoing',
}

export enum MessageStatusEnum {
  Draft = 'draft',
  Pending = 'pending',
  Sent = 'sent',
  Received = 'received',
}
